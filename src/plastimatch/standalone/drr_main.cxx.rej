--- Plastimatch/src/plastimatch/standalone/drr_main.cxx	2016-08-10 00:01:45.606660524 +0200
+++ GitLabPlastimatch/src/plastimatch/standalone/drr_main.cxx	2016-08-10 00:54:10.309959646 +0200
@@ -149,11 +149,11 @@
     }
 
     if (options->geometry_only) {
-	proj_image_save (proj, 0, mat_fn);
+	proj->save (0, mat_fn);
     } else {
 	drr_render_volume_perspective (proj, vol, ps, dev_state, options);
 	timer->start ();
-	proj_image_save (proj, img_fn, mat_fn);
+	proj->save (img_fn, mat_fn);
 	printf ("I/O time: %f sec\n", timer->report ());
     }
 
